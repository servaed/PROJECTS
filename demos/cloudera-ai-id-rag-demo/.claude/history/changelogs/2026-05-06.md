# Changelog — 2026-05-06

## Features Added

### Executive Dashboard (`/dashboard`)
- New page with 13 cross-domain KPI cards (banking: avg NPL, total credit, approval rate, avg ROI; telco: avg churn risk, critical networks, avg latency, avg packet loss; government: budget realization, avg IKM, pending services, total budget, population)
- Streaming LLM executive briefing via `POST /api/dashboard/summary` (SSE)
- Threshold alert feed with critical/warning/ok severity
- Consistent admin topbar matching setup.html / configure.html

### Monitoring Agent (`POST /api/monitor/run`)
- SSE stream of 5 automated threshold checks: NPL hotspots, critical networks, churn cities, budget underperformers, low IKM score
- Severity classification: critical / warning per check
- Integrated into dashboard page

### Anomaly Detection (`POST /api/anomaly`)
- LLM scans SQL result rows against industry benchmarks for each domain
- `AnomalyPanel` component auto-appears below SQL results when data query returns numeric columns
- Severity badges: critical (red) / warning (amber) / ok (green)

### Vision Input
- Image attachment button in InputBar; thumbnail preview before send
- `ChatRequest.image_b64` field; injected as OpenAI vision content block in `stream_synthesis()`
- Works with GPT-4o, Claude 4, Gemini and any multimodal OpenAI-compatible endpoint

### Line Charts
- Time-series detection: `/\|\s*(month|year|date|period|quarter|bulan|tahun)\s*\|/i`
- Bezier canvas line chart with gradient fill and hover snapping
- Auto-activates when SQL result has a time column

### Without AI Raw Mode
- Toggle button on assistant SQL messages: shows plain data table without LLM synthesis
- `noAI` state in `AssistantMsg`; `noai-banner` CSS indicator
- Instant side-by-side comparison: AI answer vs raw data

### Predictive Forecasting (`POST /api/forecast`)
- OLS linear regression (`_linear_regression()`) on time-series SQL results
- Projects 3 periods ahead with dashed overlay on line chart
- Returns trend direction (↑↓→), R² score, projected values
- `▷ Forecast` button appears when line chart is active

## Presentation Updates

### Slide T-07: Live Demo Guide (s16, technical-only)
- 6-card grid mapping every page URL to its capabilities
- Cards: Chat Interface `/`, Executive Dashboard `/dashboard`, Data Explorer `/explorer`, Document Intelligence `/upload`, Inference Metrics `/metrics`, Setup & Config `/setup /configure`
- Total slide count: 16 (9 business + 7 technical)

### PDF Download
- Download button (top-right, download icon); `P` keyboard shortcut
- Opens clean new window with all audience-filtered slides
- `el.style.opacity = '1'` resets animation opacity; `print-color-adjust: exact` preserves backgrounds
- Falls back to Ctrl+P for manual print (shows current slide only)

### UI Fixes
- WCAG AA contrast: dark `--t3` #475569 → #8892A4 (~5.9:1); light `--t3` #9AA0A6 → #6B7280 (~4.67:1); light `--t2` #4A5568 → #374151
- Audience toggle + theme button shifted left to accommodate PDF button (right positions: 112px / 64px / 20px)
- Cover slide tagline: "Cloudera AI Enterprise Assistant — One question, multiple angles. Policy documents, live data, geographic maps, and trend forecasts. Breaks complex enterprise data into clear, actionable light."

## Bug Fixes
- `api_dashboard_kpis`: switched from async+ThreadPoolExecutor to sync sequential queries — fixes DuckDB thread-safety and dict-row key errors
- `_run_kpi_query`: dict-aware row extraction (`isinstance(first, dict) → next(iter(first.values()))`)
- Monitoring SSE row extraction: same dict-aware fix applied

## Documentation
- README.md: new capabilities table entries, updated slide count, new Chat Modes
- CLAUDE.md: new API routes, updated Pages list, new frontend components
- PRESALES_CHECKLIST.md: new demo steps 6/7, new talking points, new known issues
- Session history written
