# Web server (React SPA + SSE streaming API)
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# Kept for local fallback / notebook use
streamlit>=1.32.0,<2.0.0

# LLM — OpenAI-compatible providers (Cloudera, OpenAI, Azure, local)
openai>=1.14.0,<2.0.0

# LLM — LangChain (used for vector store / embeddings integration)
langchain>=0.2.0,<1.0.0
langchain-community>=0.2.0,<1.0.0
langchain-huggingface>=0.0.3,<1.0.0
langchain-openai>=0.1.0,<1.0.0

# LLM — Anthropic direct API (install when LLM_PROVIDER=anthropic)
# pip install anthropic
# anthropic>=0.30.0,<1.0.0

# LLM — Amazon Bedrock (install when LLM_PROVIDER=bedrock)
# pip install boto3
# boto3>=1.34.0,<2.0.0

# Embeddings (local, no API key — supports Bahasa Indonesia)
sentence-transformers>=2.7.0,<4.0.0

# Vector store (local/demo mode — swap to Pinecone/Weaviate for production)
faiss-cpu>=1.7.4,<2.0.0

# Retrieval — BM25 keyword re-ranking for hybrid FAISS + BM25 search
rank-bm25>=0.2.2,<1.0.0

# Document loaders
pypdf>=4.1.0,<6.0.0
python-docx>=1.1.0,<2.0.0
beautifulsoup4>=4.12.0,<5.0.0
markdown>=3.5.0,<4.0.0

# SQL / structured data
pandas>=2.2.0,<3.0.0
tabulate>=0.9.0,<1.0.0

# SQL guardrails — AST-based table extraction (replaces regex approach)
sqlparse>=0.5.0,<1.0.0

# Trino query engine (used when QUERY_ENGINE=trino)
trino>=0.328.0,<1.0.0

# Object storage — MinIO / Ozone S3 gateway (used when DOCS_STORAGE_TYPE=s3)
boto3>=1.34.0,<2.0.0

# DuckDB — in-process SQL engine reading Parquet/Iceberg files (QUERY_ENGINE=duckdb)
duckdb>=1.1.0,<2.0.0

# Parquet support for Iceberg table data files
pyarrow>=15.0.0,<20.0.0

# Config and environment
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0

# HTTP client
httpx>=0.27.0,<1.0.0

# File upload (multipart/form-data)
python-multipart>=0.0.9,<1.0.0

# Token counting
tiktoken>=0.6.0,<1.0.0

# Excel parsing (for CSV/Excel → table import; .xlsx only — xlrd dropped for security)
openpyxl>=3.1.0,<4.0.0

# PDF table extraction (for Document Intelligence feature)
pdfplumber>=0.11.0,<1.0.0

# MLflow experiment tracking (optional — install to enable MLFLOW_TRACKING_URI)
# pip install mlflow
# mlflow>=2.12.0,<3.0.0

# Utilities
python-ulid>=2.2.0,<3.0.0

